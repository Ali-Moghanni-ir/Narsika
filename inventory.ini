# Optional CLI inventory. Add actual hosts and use Ansible Vault or private host vars.
# The web application builds a private inventory from its encrypted database.
[cisco_routers]

[cisco:children]
cisco_routers

[mikrotik]
